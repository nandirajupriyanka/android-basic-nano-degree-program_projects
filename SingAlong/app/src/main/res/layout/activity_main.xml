<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.priyankanandiraju.singalong.MainActivity">

    <TextView
        android:id="@+id/tv_search_song"
        style="@style/MainCategories"
        android:background="#FF8A80"
        android:drawableLeft="@android:drawable/ic_menu_search"
        android:text="@string/search_for_song" />

    <TextView
        android:id="@+id/tv_upload_audio"
        style="@style/MainCategories"
        android:background="#8BC34A"
        android:drawableLeft="@android:drawable/ic_menu_upload"
        android:text="@string/upload_audio" />

    <TextView
        android:id="@+id/tv_record_audio"
        style="@style/MainCategories"
        android:background="#00BCD4"
        android:drawableLeft="@android:drawable/ic_btn_speak_now"
        android:text="@string/record_audio" />

</LinearLayout>
